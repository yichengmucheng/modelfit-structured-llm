# 结构化大模型平台 · 面试项目介绍（AI 产品经理岗 · 产品/项目双角色版）

> **口径约定（全文已执行）：**
> 1. 不出现任何开源项目名。底层技术统一表述为**「基于大模型的表格上下文学习（In-Context Learning）」**——预训练 Transformer 将业务训练数据作为上下文（prompt）输入，无需梯度训练，单次 forward pass 直接输出预测。
> 2. 内部系统名（百擎/百基/Sigma）面试时可以说，也可以用通用说法：**建模平台（CPU 训练侧）/ 推理平台（GPU 生产侧）/ 部署登记系统 / 产品管理系统**。下文用通用说法。
> 3. 标 ⚠️ 处为建议口径，面试前替换成真实数字。
>
> **角色定位（先想清楚再开口）：** 你是这个项目的**产品经理兼项目经理**——既定义「做什么、为什么做」（PRD、决策记录、体验设计），又负责「按时做出来」（里程碑、跨团队协调、待决项拍板机制）。大厂面试里这是加分项，但要主动讲清楚两顶帽子分别做了什么，否则容易被理解为「什么都沾一点」。

---

## 一、简历版（可直接粘贴，STAR 浓缩）

**结构化大模型风控建模平台｜产品经理 & 项目经理｜2025.X – 至今**

负责公司自研「结构化大模型」平台从 1.0 到 2.0 的产品规划与项目交付：基于大模型的表格上下文学习技术，让风控分析师零算法门槛完成 建模 → 评估 → 发布 → 线上实时打分 全流程。

- **端到端产品架构**：主导打通「数据准备 → 建模平台（CPU 训练）→ GPU 推理平台 → 部署登记 → 产品上架 → 客户 API 调用」六环节交付链路，将原本训练、推理、发布各自为政的分散模块整合为引导式五步建模流程，建模周期从数周压缩至分钟级 ⚠️。
- **模型标准化交付规范（0→1）**：定义「模型文件 + 5 个自描述契约文件」的 LDM 模型包规范（特征契约 / 环境快照 / 金标准用例 / 评分映射 / 分数一致性校验），配套 SHA-256 三重一致性校验与自动合规校验器，模型跨平台迁移实现 float32 精度下打分零误差、一致性验收 100%。
- **算法特性产品化**：基于上下文学习「无梯度训练」的算法本质做出多项反直觉产品决策——指标仅展示 Test 集（避免业务方被无意义的 Train 指标误导）、超参数抽象为「简单/复杂」二选一、验证与推理合并为同页双 Tab，并统一「提示样本/验证样本」等全平台术语。
- **模型生命周期管理**：设计 9 状态模型状态机（未推理 → 推理中 → 已评估 → 发布中 → 已上线 → 已下线/已驳回等）及配套审批流，覆盖金融合规要求的可审计、可回滚。
- **项目管理**：作为项目经理协调算法、平台研发、QA、商务四方，管理「原型 → PRD → 可运行 Demo → 生产部署」四个里程碑；建立关键决策记录与待决问题清单机制，推动 bf16 精度取舍、异步导出架构等 X 项跨团队决策落地 ⚠️；主导 15 个场景的全链路压测与一致性验证方案。

---

## 二、30 秒电梯陈述

「我在金融风控公司同时担任『结构化大模型』平台的产品经理和项目经理。这个平台用基于大模型的表格上下文学习替代传统评分卡建模——把业务训练数据作为上下文喂给预训练 Transformer，不做梯度训练，分析师几分钟出一个基线风控模型。产品上我负责从 1.0 到 2.0 的规划：引导式建模流程、模型标准化交付规范、以及从建模到客户 API 调用的六环节全链路打通；项目上我协调算法、研发、QA、商务四方，按『原型 → PRD → Demo → 生产』四个里程碑推进。最终建模周期从数周降到分钟级，模型跨平台迁移做到打分零误差。」

---

## 三、业务背景

### 1. 业务场景

- 公司在金融风控领域沉淀大量数据源（身份、多头借贷、行为特征等），分析师基于这些数据建模，输出信用评分产品，用于**贷前准入、贷中监控、贷后催收**。
- 评分以**单笔 API 实时请求**（POST /score）对外服务，客户通过 ExtData 传入特征，对 P99 延迟和结果稳定性要求极高。

### 2. 平台 1.0 的问题（为什么要做 2.0）

| 痛点 | 具体表现 |
|------|---------|
| 建模门槛高、周期长 | 传统评分卡/树模型要特征工程+调参，依赖资深数据科学家，单模型数周上线 |
| 模块各自为政 | 训练、推理、发布是独立模块，分析师做完一步要自己「找下一步」，链路不连贯 |
| 交付链路断裂 | 建模平台（CPU）和生产推理平台（GPU）之间没有标准化交接，「一个模型文件打天下」，特征顺序、正类定义靠口头交接 |
| 真实事故风险 | 正类填反 → 高风险用户反得高分；float32 与 bf16 精度差约 6e-3，足以让信用分跨 4-6 个分值桶 |

### 3. 技术判断

大模型的表格上下文学习正好命中风控建模的需求形态：**中小样本（数千到数万行）、高频迭代、快速验证业务方向**。预训练 Transformer 把训练数据作为上下文，`fit` 阶段只是上下文缓存（in-context conditioning）而非更新权重，几秒到几分钟出结果。这个「训练即缓存」的本质，是后面一系列产品决策的出发点。

---

## 四、产品规划

### 1. 产品全景：四系统六环节

我负责定义并打通的端到端主链路：

```
画像/样本数据
  → 建模平台（训练 fit，产出标准模型包）
  → GPU 推理平台（加载 base，对外稳定打分）
  → 部署登记系统（商务补客户/合规信息）
  → 产品管理系统上架
  → 客户 API 调用（POST /score）
```

关键架构认知：**迁移的本质是「把训练侧产出的模型包交给推理侧加载」，而不是把已训练模型改回未训练底模**。训练侧（fit）和推理侧（base）是两种运行模式、两套负载特征（低频交互 vs 高并发实时），所以做成 CPU/GPU 双平台分离。

### 2. 2.0 版本规划：四大模块

⚠️ 按真实模块名替换：LDM 标准出包、GPU 平台迁移、Web 引导式建模、部署/API 交付。我主线跟进的是这四块的整体串联。

### 3. 用户旅程（产品主链路）

```
数据准备 → 应用推理 → 效果评估 → 发布
上传/选样本   选简单/复杂   验证样本     可选评分拉伸
指定 Y 列     看 AUC/KS    概率表       部署文档
```

每一步有明确产物：样本资产、推理报告、评估报告、API 对接文档。配套设计：

- **五步引导式建模**：步骤指示器全程可见；新建模型顺序锁定，已有模型自由跳转。
- **工作台**：快速建模/样本管理/模型管理三卡片 + 统计与近期动态，降低首次使用门槛。
- **术语统一**：「训练样本」改为「**提示样本**」——因为它本质是喂给大模型的 prompt 而非梯度训练数据；这个术语让全公司对算法本质的理解都对齐了。

### 4. 模型生命周期状态机（合规抓手）

设计 9 状态状态机：**未推理 → 推理中 → 已推理 → 评估中 → 已评估 → 发布中 → 已上线 / 已驳回 / 已下线（→ 已删除）**，每个状态绑定可操作集合（如「已评估」才能发布、「已上线」仅能调用审批锁定的模型版本）。这既是 UI 状态设计，更是金融合规要求的**审批可追溯、版本可锁定、上线可回滚**的产品化表达。

---

## 五、方案决策（面试重点：每条都是「问题 → 备选 → 取舍 → 结果」）

### 决策 1：模型交付物 = 模型文件 + 5 个自描述契约文件

- **问题**：模型文件只含推理对象本体，缺特征顺序、正类定义、打分逻辑、环境信息，跨平台迁移靠口头交接。
- **备选**：A. 维持人工交接；B. 统一转跨框架格式（成本高、生态不成熟）；C. 模型文件 + sidecar 契约文件。
- **决策**：选 C。定义 LDM 模型包 = 1 个模型文件 + 5 个 JSON：`io_schema`（特征契约）/ `source_meta`（环境快照+SHA-256）/ `test_cases`（部署验收金标准）/ `score`（概率→业务分映射，可选）/ `score_test`（训练/生产分数一致性校验）。
- **产品巧思——职责切分**：模型文件由训练引擎自动生成，分析师**只负责 5 个生成函数**保证契约内容正确，研发负责导出集成、落盘校验、上传拉包。按「能否自动获取」分层，把人工负担压到只剩业务语义（正类含义、真实样例）。

### 决策 2：一致性保障 = 金标准用例 + SHA-256 三重校验，而非肉眼比对

- bf16 与 float32 概率差约 6e-3，信用分 300-850 区间内足以跨桶。
- `test_cases.json` 记录金标准 I/O 和生成条件（dtype/device/版本），目标平台**自主回归验收**，不需要算法团队在场；SHA-256 在 meta、test_cases、文件本体三处必须一致，防篡改防错版；评分函数约束为**纯函数 + 保序 + 先量化再映射**，消除浮点尾数跳桶。
- `positive_class` 设为硬性阻断项——正类逆序是最严重的业务事故。

### 决策 3：bf16 精度取舍（parity 决策）⚠️

GPU 生产侧用 bf16 可显著提升吞吐，但概率与 float32 训练侧有偏差。我组织算法+研发做 parity 对比测试，以「分值是否跨桶」而非「概率是否绝对相等」作为业务判断标准，给出容差阈值（±6e-3）和量化打分方案，平衡了性能与一致性。⚠️（面试前确认最终结论：是保 float32 还是带容差用 bf16）

### 决策 4：只展示 Test 集指标（最能体现「懂算法」的决策）

上下文学习不做梯度训练，「训练集指标」没有传统意义，展示会让业务方系统性高估模型效果。改版后**只展示 Test 集 AUC/KS/LIFT + ROC 曲线**，并在页面说明原因；验证集比例=0 时全量 fit、不出指标（用于最终交付）。这是一个「产品理解算法本质后推翻行业惯例展示方式」的决策。

### 决策 5：复杂度产品化 + 流程合并

- 集成推理次数不直接暴露，抽象为「**简单（快）/ 复杂（稳定）**」二选一，并编码进模型目录命名规则（业务场景_架构版本_集成数_特征数_样本行数），让运维看目录名就能读出模型档案。
- 效果评估与在线推理本质只差「有无 Y 标签」，合并为同页双 Tab；导航 4 项精简为 3 项。

### 决策 6：Demo 先行、生产分轨的交付策略（项目经理决策）

推进节奏上刻意分两轨：先用轻量全栈 Demo（真实前后端 + ML 服务）快速跑通五步流程给各方对齐认知，同时生产侧按 GPU 烘焙模型 + 合规校验的正式方案并行推进。**Demo 跑通 ≠ 生产可上线**这条边界我在评审中反复明确，避免了「拿 Demo 结论直接上生产」的风险。

---

## 六、项目管理线（项目经理这顶帽子的证据）

- **里程碑管理**：算法选型 → 平台 1.0 → 2.0 规划（PRD + 模型包规范）→ 原型与 PRD 评审 → 可运行 Demo → 生产部署，逐段有明确交付物和验收标准。
- **跨团队协调**：算法（模型引擎、5 函数）、平台研发（导出集成、GPU 接入、校验器）、QA（全链路测试）、商务（部署登记、客户开通）。三方对同一件事有三套语言，我维护「三视角对照表」统一口径——比如同一个动作，业务叫「应用推理」，分析师叫 fit，研发叫上下文缓存。
- **决策与风险机制**：维护关键产品决策记录（每条含背景、备选、结论）和**待决问题清单**（如多算法下契约文件是否共用、入模特征上限是否硬拦截、租户与业务场景映射），每次评审集中拍板、责任到人。
- **质量关口**：设计 15 个场景的全链路测试方案（基准/阶梯升压/8 小时长稳/极限/突发流量/多模型并发/五类一致性/黄金样本回归），验收标准 Score 一致率 100%、概率误差 <1e-6、错误率 <0.1%。

---

## 七、项目成果

1. **效率**：建模周期数周 → 分钟级；分析师自助完成全流程，不再依赖数据科学家排期。⚠️（补：平台累计建模 X 个、覆盖 Y 个业务场景、Z 个客户接入）
2. **一致性与合规**：跨平台迁移 float32 下打分零误差；一致性验收覆盖单实例/多实例/多 GPU/高并发/跨时间五类场景，Score 一致率 100%；交付从口头交接变为自动校验 + 全链路可审计。
3. **性能** ⚠️：口径示例「单模型稳定支撑 X RPS，P99 < 200ms，单卡可部署 N 个模型」。
4. **组织资产**：LDM 模型包规范、模型状态机、决策记录机制沉淀为部门标准，后续新算法接入直接复用。

**收尾一句话**：「这个项目里我做了三层翻译：把算法特性翻译成产品规则（上下文学习无训练指标 → 只展示 Test 集、训练样本 → 提示样本）；把工程风险翻译成交付规范（精度差异 → 金标准用例 + 量化打分）；把合规要求翻译成流程设计（可审计 → 状态机 + 三重哈希 + 审批锁版本）。同时作为项目经理保证这些定义真的按里程碑落了地。这是我理解的 AI 产品经理的核心价值：**在算法、工程、业务三方之间定义标准和契约，并推动兑现**。」

---

## 八、3 分钟口述版

「我介绍一个我同时担任产品经理和项目经理、从 0 到 1 负责的项目——结构化大模型风控建模平台。

**背景**：我们公司做金融风控，分析师要基于各类数据源训练信用评分模型，以 API 实时打分服务贷前贷中贷后。1.0 时代有两个问题：一是传统建模依赖资深数据科学家、数周才能上线；二是训练、推理、发布模块各自为政，建模平台和生产推理平台之间没有标准化交接，出过高风险用户反而得高分的隐患。

**技术判断**：基于大模型的表格上下文学习正好解这个问题——预训练 Transformer 把业务训练数据当作上下文直接推理，不做梯度训练，几分钟出基线模型，天然匹配风控『中小样本、快速迭代』的节奏。

**产品规划**：我定义了六环节主链路——数据准备、建模、评估、发布、部署登记、客户 API 调用，把分散模块整合成引导式五步建模流程；设计了 9 状态的模型生命周期状态机满足金融审批合规；最核心的是定义了模型标准化交付规范：一个模型文件加五个自描述契约文件，配 SHA-256 三重校验和自动合规校验器，解决跨平台迁移的一致性问题。

**几个关键决策**可以展开：比如上下文学习没有传统训练指标，我决定平台只展示 Test 集指标，还把『训练样本』改名叫『提示样本』统一了全公司认知；比如 GPU 侧 bf16 精度和训练侧有偏差，我组织了 parity 测试，用『分值是否跨桶』作为业务判断标准来定容差。

**项目管理**上，我协调算法、研发、QA、商务四方，按原型、PRD、可运行 Demo、生产部署四个里程碑推进，维护决策记录和待决问题清单，每次评审集中拍板。

**成果**：建模周期从数周降到分钟级，模型迁移打分零误差、一致性验收 100%，交付规范沉淀为部门标准。这个项目最能体现我的价值：在算法、工程、业务之间定义标准和契约，并作为项目经理推动兑现。」

---

## 九、高频追问预案

**Q1：你说你既是 PM 又是项目经理，具体怎么分工的？团队多大？**
产品上我出 PRD、决策记录、交互原型，定验收标准；项目上我管里程碑、跨团队依赖和风险清单。⚠️ 准备真实数字：协作团队规模（算法 X 人、研发 Y 人、QA Z 人）、你直接主导 vs 参与的边界。**这题千万不能含糊，大厂最爱追「哪些是你个人拍板的」——建议准备 2 个「我拍板」的实例（如只展示 Test 集、5 函数职责切分）。**

**Q2：和 XGBoost/评分卡比，效果真的好吗？**
中小样本下与精调树模型相当甚至更好，且零调参。但核心价值不是打败最强模型，而是把数周压缩到分钟级、先快速验证业务方向，有价值的场景再投入精调。平台用 Test 集 KS/AUC/LIFT 与存量模型同口径对比。

**Q3：大模型有随机性，风控要求稳定，怎么办？**
推理是确定性 forward pass，没有采样随机性。真正的不确定来自精度（float32/bf16）、设备、环境版本——所以环境快照、金标准用例记录生成条件、回归验证保证同条件同结果，高并发下验收 Unique(Score)=1。

**Q4：为什么不微调？**
风控场景样本量中小、一个业务一个模型、迭代频繁，微调的训练和维护成本不划算；上下文学习「数据即 prompt」天然匹配「换数据快速重出」的节奏。

**Q5：模型文件里有业务数据，安全怎么考虑？**
模型包缓存了训练数据（作为上下文），所以按敏感资产管理：SHA-256 防篡改、迁移链路可审计、上线版本审批锁定、特征契约与正类定义固化存档。

**Q6：怎么衡量产品成功？**
北极星：分析师自助产出并上线的模型数。过程指标：建模周期、迁移一次通过率（合规校验通过率）、线上打分一致率、P99 延迟、客户 API 调用量。⚠️ 备 1-2 个真实数字。

**Q7：项目中最大的冲突/困难是什么？**
建议用 bf16 parity 决策或「Demo 结论能不能直接上生产」的边界之争来答：研发要吞吐、算法要精度、业务要分数稳定，我把技术分歧转换成业务判断标准（分值是否跨桶）来收敛。⚠️ 按真实经过组织。

---

## 十、为什么这样写更有竞争力（写法说明，面试前删掉）

1. **双角色立住而不模糊**：简历第一句就写「产品经理 & 项目经理」，正文分「方案决策」和「项目管理线」两条证据链，各自有具体产出，避免被理解为打杂。
2. **每个决策都是完整的取舍**：大厂 AI PM 面试的核心区分度不是「做了什么功能」，而是「面对备选怎么选、为什么」。决策 1-6 全部按「问题→备选→取舍→结果」组织。
3. **「懂算法」用产品语言证明**：不堆术语，而是展示算法本质如何改变产品规则——只展示 Test 集、提示样本改名、评分函数先量化再映射。这比背 Transformer 原理有说服力得多。
4. **端到端而非单点**：从数据准备讲到客户 API 调用和商务上架，证明你有商业化闭环视野，不只是功能 PM。
5. **量化留了钩子**：所有 ⚠️ 处是面试官必追问的数字，提前备好真实值；宁可数字小而真实，不可大而虚。
