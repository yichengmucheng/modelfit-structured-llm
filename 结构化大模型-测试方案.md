# 结构化大模型 · 全链路测试方案

---

## 1. 文档背景

当前已完成 TabPFN 模型推理引擎优化，并完成单模型性能测试。上述结果仅代表模型推理层能力。

实际业务场景中，客户通过 API 调用模型服务，请求还会经过**数据获取 → 特征处理 → 模型推理 → 结果封装**等多个环节，因此需要开展全链路测试，评估真实生产环境下的系统能力。

---

## 2. 测试目标

### 2.1 全链路性能验证
验证系统在真实业务链路下的性能表现。

- 全链路稳定 RPS 是多少
- 全链路极限 RPS 是多少
- 推荐生产运行 RPS 是多少
- P99 延迟是否满足业务要求

### 2.2 系统容量评估
评估当前部署架构能够支撑的业务规模。

- 单模型可支撑多少调用量
- 多模型同时运行时可支撑多少调用量
- 当前硬件资源利用率情况
- 是否存在扩容需求

### 2.3 瓶颈定位分析
拆解全链路耗时构成，分析以下环节，识别系统主要瓶颈：

- API 接入
- 参数校验
- 数据获取
- 特征加工
- 模型推理
- 结果封装

### 2.4 模型结果一致性验证
验证相同输入在不同时间、不同实例、不同 GPU 环境下是否产生一致结果。

- 同样输入是否始终得到相同输出
- 模型升级后结果是否发生漂移
- 多实例部署结果是否一致
- 高并发情况下结果是否稳定

---

## 3. 测试范围

### 包含范围

| 层级 | 内容 |
|------|------|
| API 服务层 | 客户调用接口：`POST /score` |
| 数据访问层 | MySQL、PostgreSQL、Redis、Elasticsearch、Hive、内部服务接口 |
| 特征加工层 | 缺失值处理、特征转换、数据映射、规则计算 |
| 模型推理层 | 真实模型推理服务（**禁止 Mock**） |
| 返回结果层 | score、probability、rank、业务标签 |

---

## 4. 测试环境

- **硬件环境**：与生产环境保持一致
- **百基部署环境**：一个模型一个 pod
- **测试数据**：建议抽取生产样本，由分析师决定（@锁晶晶）
  - 样本数量 ≥ 100 万
  - 数据分布接近生产环境

---

## 5. 测试指标

### 性能指标

| 指标 | 统计项 | 备注 |
|------|--------|------|
| 响应时间 | Avg / P50 / P90 / P95 / **P99** / Max | 重点关注 P99 |
| 吞吐量 | RPS | — |
| 错误率 | HTTP 5xx / 超时 / 业务异常 | 目标：< 0.1% |

### 资源指标（GPU）

- GPU Utilization
- 显存利用率
- PCIe 传输

---

## 6. 测试场景

### 场景一：单请求基准测试（单模型）
**目的：** 获取全链路基础耗时

| 项目 | 配置 |
|------|------|
| 并发 | 1 |
| 持续 | 10 分钟 |
| 模型数量 | 1 个 |

**输出：** 平均耗时、P99 耗时、耗时拆解明细（API / 数据 / 特征 / 推理 / 返回，仅看百基耗时）

---

### 场景二：阶梯升压测试（单模型）
**目的：** 寻找系统稳定运行区间

| 项目 | 配置 |
|------|------|
| 并发梯度 | 10 → 20 → 40 → 80 → 120 → 160 → 200 |
| 每档持续 | 10 分钟 |
| 模型数量 | 1 个 |

**输出：** RPS、P99、错误率、GPU 利用率
**最终输出：** 推荐生产运行并发、推荐生产运行 RPS、稳定运行区间（Sweet Spot）

---

### 场景三：长稳测试（单模型）
**目的：** 验证长期稳定运行能力（基于 GPU 模型网关进行压测，亦庄机房）

| 项目 | 配置 |
|------|------|
| 并发 | 20 |
| 持续 | 8 小时 |
| 模型数量 | 1 个，N=4 |

**观察项：** 内存泄漏、显存增长、线程堆积、连接池耗尽、GC 异常波动

---

### 场景四：极限压测（单模型）
**目的：** 寻找系统极限容量边界（若场景二能达到 RPS 1000，则本场景不验证）

| 项目 | 配置 |
|------|------|
| 流量梯度 | 250 → 400 → 600 → 800 → 1000 → 1500 → 2000 |
| 模型数量 | 1 个 |

**停止条件（满足任一）：** 错误率 > 1%，P99 > 500ms，GPU/CPU 资源耗尽

**输出：** 系统极限 RPS、系统极限并发

---

### 场景五：突发流量测试（单模型）
**目的：** 验证系统抗流量突刺能力

| 项目 | 配置 |
|------|------|
| 基线流量 | 100 RPS |
| 突发流量 | 1000 RPS |
| 模型数量 | 1 个 |

**观察项：** 是否触发降级策略、队列堆积情况、拒绝策略行为、恢复时间

**恢复时间要求：** ≤ 60 秒恢复至稳定区间

---

### 场景六：多模型并发测试（多模型）
**目的：** 验证多模型共享 GPU 资源下的吞吐与隔离能力

| 项目 | 配置 |
|------|------|
| 模型数量 | 4 个（N=1 × 2 + N=4 × 2） |
| 子场景 | 扩充为 2 个场景（待补充） |

---

### 场景七：单实例一致性测试（单模型）
**目的：** 验证相同输入在单实例内多次调用结果一致性

| 项目 | 配置 |
|------|------|
| 固定样本 | 1000 条 |
| 循环调用 | 1000 次 |
| 模型数量 | 1 个 |

**验收标准：** Score 一致率 100%，Probability 误差 < 1e-6

---

### 场景八：跨时间一致性测试（单模型）
**目的：** 验证服务重启 / 时间变化不影响推理结果

**方法：** 分别在当天、次日、服务重启后执行

**验收标准：** 输出完全一致，Score 一致率 100%，Probability 误差 < 1e-6

> **约束（必须）：** 测试期间禁止模型版本变更、推理引擎升级、CUDA/驱动变化、配置变更

---

### 场景九：多实例一致性测试（单模型）
**目的：** 验证多服务实例间输出一致性

**方法：** 同样本请求两轮，对比一致性，观察日志看请求进入 istio

**验收标准：** 输出完全一致，Score 一致率 100%，Probability 误差 < 1e-6

---

### 场景十：多 GPU 一致性测试（单模型）
**目的：** 验证不同 GPU 卡推理一致性

**方法：** 固定样本分别调度至 GPU0 ~ GPU7

**验收标准：** Score 一致率 100%，Probability 误差 < 1e-6

---

### 场景十一：高并发一致性测试（单模型）
**目的：** 验证高并发情况下结果稳定性

| 项目 | 配置 |
|------|------|
| 固定样本 | — |
| 并发 | 20 |
| 持续调用 | 10000 次 |
| 模型数量 | 1 个 |

**验收标准：** Unique(Score) = 1，Probability 无漂移，无并发异常写入问题

---

### 场景十二：高并发一致性测试（多模型）
**目的：** 验证高并发情况下多模型结果稳定性

| 项目 | 配置 |
|------|------|
| 并发 | 20 |
| 持续调用 | 10000 次 |
| 模型数量 | 多模型 |

**验收标准：** Unique(Score) = 1，Probability 无漂移，无并发异常写入问题

---

### 场景十三：模型回归测试（单模型）
**目的：** 验证模型 / 引擎升级后结果不发生漂移

**黄金样本集：** 1000 条固定样本，每条记录 sample_id、expected_score、expected_probability

**触发时机：** 模型升级、推理引擎升级、CUDA/驱动升级、架构调整

**验收标准：** Score 一致率 100%，Probability 误差 < 1e-6，无系统性偏移（drift）

---

### 场景十四：单卡部署模型极限
**目的：** 验证单卡最大部署模型数量

| 项目 | 配置 |
|------|------|
| 固定样本 | 1000 条 |
| 循环调用 | 1000 次 |
| 模型数量 | N 个（逐步增加） |

**验收标准：** Score 一致率 100%，服务可用不报错，P99 < 200ms

---

### 场景十五：GPU 掉卡模拟
**目的：** 待补充

**方法：** 待补充

**验收标准：** 待补充

---

## 7. 测试交付物

| 交付物 | 包含内容 |
|--------|---------|
| 性能测试报告 | 稳定 RPS、极限 RPS、推荐运行 RPS、推荐并发数、P99 延迟 |
| 资源利用率报告 | GPU 利用率、显存利用率、CPU 利用率、内存利用率 |
| 链路耗时分析报告 | 数据获取 / 特征处理 / 模型推理 / 结果封装耗时占比，形成瓶颈分析结论 |
| 模型一致性报告 | 单实例一致率、多实例一致率、多 GPU 一致率、高并发一致率 |

---

## 8. 最终验收标准

待与相关方沟通后补充。

---

*结构化大模型全链路测试方案 · 草稿*
