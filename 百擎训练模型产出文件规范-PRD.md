# 百擎训练模型产出文件规范
## 1. 文档目的
规范百擎平台训练完成后产出的离线模型文件结构，保证模型从百擎平台迁移到百基平台后打分结果一致。
## 2. 背景与现状

### 2.1 现状描述

百擎平台基于 TabPFN 完成模型训练，产出一个 `.pkl` 文件，现需将训练链路迁移至百基平台，但百擎产出的单 pkl 文件无法在百基直接打分

### 2.2 变更原因

百擎产出文件需满足百基平台的接入要求，并保证打分一致性。

## 3. 目标与范围

### 3.1 业务目标

模型跨平台迁移后，百基平台打分结果与百擎平台保持一致。

### 3.2 技术目标

分析师调用函数直接生成LDM标准离线文件包：**1 个 pkl + 5 个自描述文件**（io_schema.json、source_meta.json、test_cases.json、score.py、score_test.py），对文件内容与格式进行统一规范，供百基平台读取使用。

### 3.3 业务范围

| 系统模块 | 变更类型 | 说明 |
|----------|----------|------|
| 1 个 pkl + 5 个自描述文件直接生成 | 规范新增 | 分析师调用生成函数，产出标准离线文件包 |
| 百擎评分拉伸交互逻辑 | 规范新增 | 百擎平台在**模型训练完成后的导出阶段**（TBD，待与研发确认）设置以下交互逻辑：① 用户选择是否需要拉伸；② 用户填写值域区间（如 300-999）；③ 用户填写方向说明（值越大，风险越___）；④ 平台调用拉伸函数；⑤ 生成最终 score.py |

## 4. 需求描述

### 4.1 功能需求

#### 4.1.1 模型文件目录结构

百擎产出的 pkl 文件与函数生成的 5 个自描述文件同级存放，构成一个完整的离线文件包：

```
raw/<business_scene>_tab<arch>_n<N>_f<F>_<rows>/
├── <同名>.pkl            # TabPFN 训练产出
├── io_schema.json        # 定义输入特征与输出标签
├── source_meta.json      # 记录环境版本与模型形状
├── test_cases.json       # 金标准验证用例
├── score.py              # 概率→整数分值映射（按需，有评分需求时提供）
└── score_test.py         # score.py 自测（有 score.py 则必配）
```

**目录命名字段说明：**

| 字段 | 示例 | 取值规则 |
|------|------|----------|
| `business_scene` | `credit_risk` | 取系统配置的业务场景名称（小写+数字+下划线） |
| `arch` | `2.5` | 当前固定为 `2.5` |
| `N` | `4` | 取百擎模型复杂度字段：简单=1，复杂=4 |
| `F` | `147` | 取百擎系统经过筛选后的实际特征数量 |
| `rows` | `6000` | 取百擎系统对应的训练样本量（精确整数，不用 k 简写） |


#### 4.1.2 io_schema.json

定义模型推理时的输入特征列表及输出类别标签。**inputs 的顺序即为模型特征输入顺序**，百基平台须严格按此顺序传参。

```json
{
  "schema_version": 1,
  "inputs": [
    {
      "name": "age",
      "dtype": "numeric",
      "required": true,
      "missing_allowed": true
    },
    {
      "name": "city",
      "dtype": "categorical",
      "required": true,
      "missing_allowed": true,
      "categories": ["bj", "sh", "gz", "other"]
    }
  ],
  "outputs": {
    "classes": [0, 1],
    "positive_class": 1,
    "positive_class_meaning": "default/bad",
    "n_classes": 2
  }
}
```

**inputs 字段说明：**

| 字段 | 示例 | 取值规则 |
|------|------|----------|
| `name` | `age` | 取入模变量名称 |
| `dtype` | `numeric` | 数值型特征取 `numeric`，类别型特征取 `categorical` |
| `required` | `true` | 当前固定为 `true` |
| `missing_allowed` | `true` | 当前固定为 `true` |
| `categories` | `["bj","sh","gz","other"]` | 仅 categorical 类型存在，取该特征的全量枚举值 |

**outputs 字段说明：**

| 字段 | 示例 | 取值规则 |
|------|------|----------|
| `classes` | `[0,1]` | 取模型输出的全部类别标签 |
| `positive_class` | `1` | 取正样本对应的类别标签 |
| `positive_class_meaning` | `default/bad` | 取正样本对应的业务含义 |
| `n_classes` | `2` | 取类别总数 |


#### 4.1.3 source_meta.json

#### 4.1.4 test_cases.json

#### 4.1.5 score.py 

将正类概率映射为整数分值。**没有评分需求时不需要此文件**，目录结构和 test_cases 中的 `expected_score` 字段同步省略。

**百擎交互逻辑（分析师在导出阶段配置）：**

1. 用户选择是否需要分值输出
2. 用户填写值域区间，如 `300 → 999`
3. 用户填写方向：值越大，风险越 ___（高/低）
4. 平台调用拉伸函数
5. 给出分值

本节详细讲解新版评分拉伸（stretch）逻辑的数学原理和实际做法：

根据最新产品需求，拉伸分值过程不是简单的线性变换，而是要求对「正类概率」与「目标分值区间」间的映射关系进行拟合，得到二次型映射公式，更好地契合业务在不同概率分段下的分值分布需求。具体逻辑如下：

### 一、主要参数与操作流程

- 用户导出阶段，会填写3个关键拉伸参数：
  - `A`, `B`：分值区间的最小、最大值（如 A=300, B=999）
  - `High`：分值方向说明，例如「分值越大风险越高」或「分值越大风险越低」等（影响映射方向）
- 系统获取模型在**训练集和验证集**上的正类概率分布（所有样本），纵向拼接成大样本对
- 用上述所有样本点 \((p, s)\)，其中 \(p\) 是正类概率 \(\in [0,1]\)，\(s\) 是目标分值（按区间和映射方向指定），
  拟合一个二次多项式曲线：\(score(p) = a p^2 + b p + c\)
- 得到映射系数 \(a, b, c\)，最终推理时直接将输出概率套入公式生成分值

### 二、数学公式说明

假设
- 输入概率 \(p\)，\(p \in [0,1]\)
- 目标分值区间为 \([A, B]\)，如 [300, 999]
- 最终分值计算公式为：

\[
score(p) = a p^2 + b p + c
\]

其中 \(a, b, c\) 的确定步骤如下：

1. **准备拟合数据：**
   - 收集训练集与验证集所有样本的正类概率 \(p_i\)。
   - 按业务指定分值区间 \([A, B]\) 与方向 High/Low，为 \(p=0\) 和 \(p=1\) 明确指定映射端点（如“分值越大风险越高”则 \(p=0\) 映射A, \(p=1\)映射B）。
   - Optionally，可对中间分位点（如 \(p=0.5\)）按业务约束指定希望的参考分值，从而让拟合更加灵活。

2. **二次拟合：**
   - 对所有点 \((p_i, s_i)\)，用最小二乘法拟合二次函数，使得 \(\sum_{i} (a p_i^2 + b p_i + c - s_i)^2\) 最小。
   - 常见做法也可只用端点和中点，解下面线性方程组（例）：

     \[
     \begin{cases}
     score(0) = c = S_0 \\
     score(1) = a + b + c = S_1 \\
     score(0.5) = 0.25a + 0.5b + c = S_{0.5}
     \end{cases}
     \]
     其中 \(S_0 =\) A 或 B，依赖 High 参数，\(S_1 =\) B 或 A，\(S_{0.5} =\) 业务自定义或区间中值

   - 解出唯一一组 \(a, b, c\)，即可确定最终分值公式。

### 三、业务拉伸实质

- 本质上是“概率到分值的二次映射”，即模型输出概率→业务分值，允许更细致地调控分值区间内分布形状。
- 拟合点越多，越能贴合真实业务分值期望
- 公式确定后，评分函数为：
  > 对任意概率 \(p\)，输出分值为 \(score(p) = a p^2 + b p + c\)。处理前一般会对 \(p\) 保留6位小数（量化），保证跨平台一致性。
  > 计算结果一般四舍五入为整数，最终限制在 [A, B] 内。

### 四、约束与契约

- 若业务指定「分值越高风险越高」，则 \(p\uparrow \Rightarrow score\uparrow\)；「分值越高风险越低」则反向
- score 必须为纯函数，且全域定义于 \(p\in[0,1]\)
- score 结果必须落在分值区间 [A,B] 内，非法概率抛异常

---

**小结**：

- 拉伸分值本质是对概率 \(p\) 拟合一个二次公式：\(score(p) = a p^2 + b p + c\)
- 系数 \(a,b,c\) 由历史样本概率及指定的区间/方向通过最小二乘法或端点插值确定
- 映射曲线可灵活调整，不再局限于线性
- 便于表达“低概率压低，高概率拉高”等分值分布需求
- 用量化-拟合-映射组合，保证各平台分值输出一致可控

#### 4.1.6 score_test.py — 分值测试（有 score.py 则必配）

对 score.py 的正确性进行自动验证，包含：
- 通用契约测试（保序、纯函数、全域、范围、量化稳定性）
- 金标准回归：若 test_cases.json 中有用例，则断言 `stretch(expected_proba[正类列]) == expected_score`

> 具体实现由分析师提供（待补充，预计 2026-06-04）。

---

### 4.2 合规校验规则

**硬性（任一不通过则拒绝接入百基）：**

| 校验项 | 规则 |
|--------|------|
| 必备文件齐全 | pkl + io_schema.json + source_meta.json + test_cases.json 四文件均存在 |
| 特征数一致 | `len(io_schema.inputs)` == `source_meta.shape.n_features` |
| sha256 三重一致 | `source_meta.pkl_sha256` == `sha256(pkl)` == `test_cases.generated_with.pkl_sha256` |
| expected_proba 维度 | 每个 case 的 `expected_proba` 长度 == `n_classes` |
| score 结构 | 有 score.py 则必须有 score_test.py，且 `positive_class` 与 io_schema 一致 |

**软性（警告但不阻塞）：**

| 校验项 | 规则 |
|--------|------|
| categorical 类别枚举 | 类别特征需填写完整 categories 集合 |
| test_cases 非空 | 建议提供真实业务样本 |
| positive_class_meaning | 填写正类业务含义 |

---

### 4.3 百基平台接入要求

百基平台读取离线文件包时需满足：

1. **特征顺序**：严格按 `io_schema.inputs` 的顺序传入特征，不可乱序
2. **特征类型**：对 categorical 特征按 `categories` 中定义的枚举编码，不可自行推断顺序
3. **推理环境**：`n_estimators` 须与 `source_meta.shape.n_estimators` 一致
4. **精度对齐**：推理 dtype 须与 `test_cases.generated_with.dtype` 一致（当前为 float32）
5. **验证方式**：加载模型后，用 test_cases.json 中的用例跑一遍，对比结果是否在 tolerance 范围内

---


