# 百擎平台 · LDM 离线模型文件规范

**文档编号：** LDM-TN-2026-001
**文档类型：** 产品需求文档（PRD）
**文档状态：** 草稿 · 待评审
**创建日期：** 2026-05-28
**需求提出方：** 业务方 / 算法团队

> **LDM-TN 编号说明：** 本文档在版本记录时使用「LDM-TN」作为固定前缀，后接流水号（如 LDM-TN-001）。前缀不随内容变化，是变更单的唯一标识锚点。

---

## 1. 文档目的

本文档定义百擎平台产出的离线模型文件（LDM，Loaded Model）在从**百擎平台（CPU 建模）迁移至百基平台（GPU 推理部署）**过程中所需遵循的**目录结构、文件命名、附属配置（sidecar）内容**及**合规校验规则**，确保模型迁移后打分行为可复现、可审计、可自验证。

---

## 2. 背景与现状

### 2.1 现状描述

**业务背景：**

百融在金融风控领域拥有大量数据源（用户身份信息、多投数据、各类行为特征等）。分析师（算法工程师）基于这些数据训练模型，最终输出信用评分产品（如融智分、融安分），应用于贷前准入、贷中监控、贷后催收等场景。评分以单笔 API 请求方式实时响应，结果以报文形式返回调用方。

**平台现状：**

百擎平台基于 TabPFN 开源项目构建。TabPFN 是一种专为中小规模表格数据设计的预训练 Transformer 基础模型，通过上下文学习（ICL）机制，将业务训练数据作为 prompt 输入，无需梯度训练即可快速完成推理，适用于样本量 ≤ 50,000、特征数 ≤ 700 维的分类任务。

分析师在百擎平台完成以下工作：

1. 上传样本数据（X，Y），样本量不超过 50,000，特征不超过 700 维
2. 调用 TabPFN 训练模型，平台以固定脚本输出 KS、Lift 等效果指标及分布图表
3. 百擎生成一个 `.pkl` 文件作为模型产物

**核心矛盾：**

TabPFN 天然适合 GPU 推理（单次 forward pass 处理整个上下文，GPU 并行加速效果显著）。当前模型部署在 CPU 平台（百擎），性能受限。为此，百融建设了**百基平台**（GPU 推理平台），目标是将百擎训练的模型迁移至百基进行线上实时打分，以提升推理性能。

**当前 pkl 交付的问题：**

| 问题编号 | 问题标题 | 问题描述 |
|----------|----------|----------|
| P-01 | 信息不完整 | `.pkl` 仅保存 Transformer 推理对象本体，不包含特征顺序、正类定义、打分逻辑等部署契约，跨平台迁移依赖人工口头交接，易出错 |
| P-02 | 可复现性差 | 缺乏环境快照记录（Python / TabPFN / PyTorch 版本），CPU 与 GPU 平台浮点计算差异（float32 vs bf16，误差约 6e-3）可能导致分值不一致 |
| P-03 | 无自验证手段 | 无金标准测试用例，百基平台无法自主验证模型加载后打分是否与百擎一致 |
| P-04 | 打分逻辑散落 | 概率到整数分值的映射（Score.py）未随模型文件交付，存在正类逆序风险（高风险用户反得高分） |

### 2.2 变更动因

- **性能驱动：** TabPFN 天然适合 GPU 推理，百基平台（GPU）相比百擎（CPU）推理性能大幅提升，满足线上单笔实时请求的延迟要求。
- **一致性驱动：** 模型从百擎迁移到百基后，必须保证两个平台的打分结果完全一致，需要标准化的验证机制。
- **合规驱动：** 金融风控场景下，模型输出需可审计，特征定义、正类含义、版本信息须随模型固化存档。
- **工程驱动：** 解决「一个 pkl 打天下」导致的信息丢失问题，由分析师配置 5 个自述文件（io_schema / source_meta / test_cases / score.py / score_test.py），构建可自验证的标准化交付物。

## 3. 目标与范围

### 3.1 业务目标

- 模型跨平台迁移后，打分结果与源平台完全一致（float32 精度下零误差）。
- 业务方可通过 `test_cases.json` 自主验证目标平台模型是否加载正确，无需依赖算法团队在场。
- 特征契约、正类定义随模型文件固化，消除口头交接导致的特征顺序 / 正类逆序风险。

> **注意：** `n_estimators` 的设置值（1 或 4）直接影响推理速度与结果质量，源平台与目标平台必须保持一致。**迁移前须向强哥确认当前 `n_estimators` 值**并写入 `source_meta.json`。

### 3.2 技术目标

- 定义 LDM 标准目录结构与 4 类 sidecar 文件（`io_schema` / `source_meta` / `test_cases` / `Score.py`）。
- 实现 `pkl_sha256` 三重一致性校验（`source_meta` ↔ `test_cases` ↔ pkl 文件本体）。
- 提供合规校验器，自动检测必备件完整性与字段一致性。

### 3.3 变更范围清单

| 系统模块 | 变更类型 | 说明 |
|----------|----------|------|
| LDM 离线模型文件包 | 规范新增 | 制定跨平台统一的文件目录结构、命名约定、sidecar 文件内容规范 |
| io_schema.json | 规范新增 | 输入特征契约（名称、顺序、类型、必填、缺失值规则）及输出标签契约 |
| source_meta.json | 规范新增 | 模型版本、形状、环境快照、pkl 哈希校验 |
| test_cases.json | 规范新增 | 金标准输入输出用例，供合规校验与平台间一致性验证 |
| Score.py / Score_test.py | 软性可选 | 概率→整数分值映射函数及其合规自测（仅需分值输出的业务场景使用） |
| 百擎平台合规校验器 | 功能优化 | 自动校验必备文件完整性、字段三重一致性、形状对齐 |

---

## 4. 需求描述

### 4.1 功能需求

#### 4.1.1 核心理念：一个可用的离线模型 = pkl + 部署契约

**pkl 是什么？**

`.pkl`（Pickle）是 Python 的二进制序列化文件格式，用于将 **Transformer 推理对象（基于深度学习）** 完整保存到磁盘。加载后可直接调用 `predict()` 进行推理。

> **说明：** TabPFN 是预训练好的 Transformer 神经网络，`fit()` 不更新网络权重，而是将业务训练数据作为上下文（prompt）缓存进来。因此 pkl 里保存的是：预训练 Transformer 权重引用 + 业务训练数据（预处理后的张量）+ 预处理管道配置。

但 pkl **不包含模型的部署与打分契约信息**，具体缺失：

- 特征名称与顺序（Transformer 对特征列顺序敏感，顺序变了结果就变）
- 哪个输出类别是业务"正类"（正类填错会导致高风险用户反得低分）
- 如何将概率转为整数分值
- 训练时的运行环境版本（PyTorch / Python / TabPFN 版本）
- 推理时的数值精度（float32 / bf16）与推理设备（CPU / GPU）

这些缺失信息必须通过 sidecar 文件额外附带。

**按「能否从 pkl 自动获取」分三层：**

| 层级 | 类型 | 内容 | 填写方式 |
|------|------|------|----------|
| ① | 内在（工具自动填） | pkl 本体、特征名+顺序（`feature_names_in_`）、输出类别（`classes_`）、形状（`n_features` / `n_train_rows`）、sha256 | 直接从 pkl 读取，工具自动填 |
| ② | 可推导（带前提） | 环境版本（当前 env 读取，单环境下等于训练值）、特征类型（默认 numeric） | env + 约定，工具自动填 |
| ③ | 不存在于 pkl（人工填写） | 正类定义及业务含义、Score.py 打分逻辑、输出获取策略、test_cases 真实业务样例 | **业务方 / 算法团队手工填写** |

---

#### 4.1.2 目录结构与命名规范

所有文件统一放置在同一目录下，sidecar 与 pkl 共置：

```
raw/<business_scene>_tab<arch>_n<N>_f<F>_<rows>/
├── <同名>.pkl          # 拟合的 TabPFNClassifier（核心产物）
├── io_schema.json      # 输入特征契约 + 输出标签契约
├── source_meta.json    # 版本 + 形状 + sha256 + 环境快照
├── test_cases.json     # 金标准 I/O（可为空数组）
├── Score.py            # 概率→整数分值映射（软性，仅需分值时使用）
└── Score_test.py       # Score.py 合规自测（有 Score.py 则必配）
```

**目录命名约定（软性，不匹配只警告，不阻塞合规）：**

```
正则：^[a-z][a-z0-9_]*_(tab\d+\.\d+)_n(\d+)_f(\d+)_(\d+)$

示例：credit_risk_tab2.5_n1_f147_6000
      ↑业务场景        ↑架构版本 ↑估计器数 ↑特征数 ↑训练行数
```

各段含义：

| 段 | 示例 | 含义 |
|----|------|------|
| `business_scene` | `credit_risk` | 业务场景名，小写+数字+下划线 |
| `tab<arch>` | `tab2.5` | 架构版本号（注意：≠ tabpfn 包版本） |
| `n<N>` | `n1` | 估计器数量（n_estimators） |
| `f<F>` | `f147` | 特征数（精确整数） |
| `<rows>` | `6000` | 训练行数（精确整数，不使用 k 简写） |

> **软性说明：** 目录名仅为「认知编码」，meta 文件是 ground truth。例如目录名写 `f150`，但实际提取后模型真实特征数可能是 147，以 `source_meta.json` 中的值为准。`n_features` 与 `n_train_rows` 随每次训练的入参数据不同而变化，须重新记录。

---

#### 4.1.3 io_schema.json — 输入特征契约 + 输出标签契约

定义模型推理时的输入特征列表（列表顺序即特征顺序）及输出类别标签。所有字段均需与训练时保持一致，不可在目标平台自行修改。

**inputs 字段说明（每个特征的定义）：**

| 字段名 | 类型 | 含义说明 |
|--------|------|----------|
| `name` | string | 特征名称，与训练时 DataFrame 列名完全一致。例："age"、"city" |
| `dtype` | `"numeric"` \| `"categorical"` | 特征类型。`numeric`=数值型（整数/浮点数）；`categorical`=类别型（字符串枚举）。默认全为 numeric，需业务方标注类别列 |
| `required` | boolean | 该特征在打分时是否为必传字段。`true`=必须传入，缺失则拒绝请求；`false`=可不传 |
| `missing_allowed` | boolean | 该特征是否允许值为 null/NaN（即已传入但值缺失）。TabPFN 原生支持缺失值，不需要人工填充 |
| `categories` | string[]（仅 categorical） | 类别特征的全量枚举值，例：`["bj","sh","gz","other"]`。防止目标平台编码顺序不一致导致分数逆转 |

**完整示例（含类别特征）：**

```json
{
  "schema_version": 1,
  "inputs": [
    {
      "name": "age",
      "dtype": "numeric",
      "required": true,
      "missing_allowed": true
    },
    {
      "name": "city",
      "dtype": "categorical",
      "required": true,
      "missing_allowed": false,
      "categories": ["bj", "sh", "gz", "other"]
    }
  ],
  "outputs": {
    "classes": [0, 1],
    "positive_class": 1,
    "positive_class_meaning": "违约/坏",
    "n_classes": 2
  }
}
```

> ⚠️ **positive_class 是关键防护：** 指定哪个 class 是业务正类，Score.py 必须使用此值决定取哪一列概率。若正类定义错误，高风险用户将得到低分，产生严重业务事故。

**outputs 字段说明：**

| 字段 | 说明 |
|------|------|
| `classes` | 模型输出的全部类别，列表顺序与 `predict_proba` 返回的列对齐 |
| `positive_class` | 业务正类的类别值，Score.py 依据此值取对应列的概率 |
| `positive_class_meaning` | 正类的业务含义描述，与 `source_meta.naming.business_scene` 共同构成完整的业务语义。`business_scene` 说明预测场景是什么（如 `credit_risk`），`positive_class_meaning` 说明在该场景下预测目标是什么（如"违约/坏客户"）。两者配合，让接手人无需口头交接即可理解模型用途 |
| `n_classes` | 类别总数，用于校验 `expected_proba` 数组长度 |

---

#### 4.1.4 source_meta.json — 版本 / 形状 / 环境快照

记录模型生成时的完整环境快照，用于目标平台环境兼容性验证，以及 `pkl_sha256` 三重一致性校验。

**字段说明：**

**▍顶层字段**

| 字段 | 填写方式 | 说明 |
|------|----------|------|
| `pkl_sha256` | 工具自动计算 | pkl 文件的 SHA-256 哈希值（64位十六进制）。三重一致性校验锚点，不可手填 |
| `captured_at` | 工具自动填充 | 生成时间戳，ISO 8601 格式，如 `2026-05-26T12:00:00Z` |
| `env_capture` | 工具自动填充 | 环境说明，单环境工作流固定填 "单环境工作流：训练环境 == 捕获环境" |

**▍source 对象（运行环境版本）**

| 字段 | 填写方式 | 说明 |
|------|----------|------|
| `kind` | 固定值 | 固定填 `"tabpfn"` |
| `tabpfn_version` | 自动填充（当前环境读取） | 训练时使用的 TabPFN 包版本，如 `6.3.2` |
| `sklearn_version` | 自动填充 | scikit-learn 版本，如 `1.6.1` |
| `numpy_version` | 自动填充（**随环境变化**） | NumPy 版本，如 `2.x`。不同环境版本不同，迁移前需确认目标环境兼容性 |
| `python_version` | 自动填充（**随环境变化**） | Python 版本，如 `3.12.7`。影响 pkl 序列化兼容性，跨主版本迁移需重新序列化 |
| `torch_version` | 自动填充（**随环境变化**） | PyTorch 版本，如 `2.x`。TabPFN 底层 Transformer 依赖，版本差异可能影响推理结果 |
| 其他环境依赖 | **按需自行补充** | `source` 对象不限于以上字段，如业务环境还依赖其他库（如 `pandas_version`、`cuda_version` 等），可自行追加，字段命名保持 `<库名>_version` 格式 |

**▍shape 对象（模型形状）**

| 字段 | 填写方式 | 说明 |
|------|----------|------|
| `n_estimators` | **需向强哥确认** | 集成推理次数（**1 或 4**）。值越大结果越稳定但推理越慢。源/目标平台必须一致，否则结果不可比 |
| `n_features` | **随入参数据变化** | 训练集的特征列数。实际提取特征数（如147）与目录名中的 `f150` 可能不同，以 meta 为准 |
| `n_train_rows` | **随入参数据变化** | 训练集行数（精确整数）。即放入 Transformer 上下文 prompt 的样本行数 |
| `device` | **待定（当前先填 cpu）** | 推理设备。CPU 与 GPU 精度存在细微差异，需与 `test_cases.generated_with.device` 保持一致 |

**▍naming 对象（目录名编码，仅作认知参考，meta 为 ground truth）**

| 字段 | 填写方式 | 说明 |
|------|----------|------|
| `business_scene` | 人工填写 | 业务场景名，与目录名前缀一致，如 `credit_risk` |
| `arch` | 人工填写 | Transformer 架构版本号，如 `2.5`（≠ tabpfn 包版本） |
| `n` | 与 `n_estimators` 一致 | 目录名中 `n<N>` 段的数值 |
| `f` | 与 `n_features` 一致 | 目录名中 `f<F>` 段的数值（软性，可与实际特征数不同） |
| `rows` | 与 `n_train_rows` 一致 | 目录名中 `<rows>` 段的数值 |

**完整示例：**

```json
{
  "schema_version": 1,
  "pkl_sha256": "<64位十六进制哈希>",
  "captured_at": "2026-05-26T12:00:00Z",
  "env_capture": "单环境工作流：训练环境 == 捕获环境",
  "source": {
    "kind": "tabpfn",
    "tabpfn_version": "6.3.2",
    "sklearn_version": "1.6.1",
    "numpy_version": "2.x",
    "python_version": "3.12.7",
    "torch_version": "2.x"
    // 如有其他环境依赖，在此处自行追加，如 "pandas_version": "2.x"
  },
  "shape": {
    "n_estimators": 1,
    "n_features": 147,
    "n_train_rows": 6000
  },
  "naming": {
    "business_scene": "credit_risk",
    "arch": "2.5",
    "n": 1,
    "f": 147,
    "rows": 6000
  }
}
```

> ⚠️ **n_estimators 确认项：** 该值决定集成成员数量，影响推理速度与结果质量。平台可能设置为 1（快速/简单）或 4（稳定/复杂），源平台与目标平台必须一致。**写文档并打包前需向强哥确认实际设置值。**

---

#### 4.1.5 test_cases.json — 金标准 I/O 验证用例

记录在源平台生成的真实输入输出样例，供目标平台加载模型后自主验证推理结果是否一致。文件必须存在，内容可为空数组（`"cases": []`）。

**三条核心规则：**

**① expected_proba 的格式约束**

`expected_proba` 数组的列顺序必须与 `io_schema.outputs.classes` 对齐。正类所在列由以下公式确定：

```
正类列索引 = classes.index(positive_class)

示例：classes = [0, 1]，positive_class = 1
→ 正类列索引 = 1
→ expected_proba = [0.31, 0.69] 中，0.69 是正类概率
→ score.py 取 expected_proba[1] = 0.69 转分值
```

**② 生成条件必须记录（dtype / 版本 / device）**

Transformer 的概率输出对数值精度极其敏感：

- `float32` vs `bf16`：同一输入，概率差异约 **6e-3（0.006）**。若分值区间为 300-999（共700格），每格宽约 0.001，则 0.006 的误差可跨 **4-6 个分值桶**，直接导致跨平台分值不一致
- 因此 `generated_with` 中的 `dtype` 和 `device` 必须如实记录，目标平台复现时必须使用相同条件

**③ 文件角色与空用例说明**

`test_cases.json` 的定位是该模型 pkl 文件的**自参考 / 自验证数据**：

- 可为空（`"cases": []`）：当暂无真实业务样例时，文件结构必须存在，cases 留空即合规（软性要求）
- 业务方可通过 `--sample-inputs` 参数后续补填真实样例
- 本轮不作为硬约束强制校验，但这些用例是后续 `.vldm` 奇偶校验（parity check）的种子数据，建议尽早填写

---

**标准格式：**

> 平台侧提供「输出概率 / 输出整数分值」的显示选择，test_cases.json 本身格式统一，无需区分场景。`expected_score` 仅在配置了 Score.py 时填写，否则省略；`tolerance.score_exact_fp32` 同理。

```json
{
  "schema_version": 1,
  "generated_with": {
    "pkl_sha256": "<64位哈希>",
    "tabpfn_version": "6.3.2",
    "dtype": "float32",
    "device": "cpu"
  },
  "tolerance": {
    "proba_abs_fp32": 0.0,
    "proba_abs_bf16": 6e-3,
    "score_exact_fp32": true
  },
  "cases": [
    {
      "input": {"age": 35, "city": "bj"},
      "expected_proba": [0.31, 0.69],
      "expected_score": 642
    }
  ]
}
```

> **device 字段待定说明：** CPU 与 GPU 推理结果存在细微精度差异。当前先记录为 `"cpu"`，目标平台必须使用相同设备类型复现才能通过校验。正式确定后更新此字段。

---

#### 4.1.6 Score.py + Score_test.py — 概率转分值（软性可选）

> ✅ **按需使用：** 仅当业务场景需要将模型概率转换为整数分值（如信用评分卡 300-999 分）时，才需要 Score.py。**如果业务只需要概率输出，可跳过此文件，目录结构中也不需要包含。** 上述目录结构与 test_cases 格式相应调整（去掉 `expected_score`）。

Score.py 将概率映射为整数分值，是**纯函数**（同输入同输出，无副作用）。一旦提供，`Score_test.py` 为必配项，且契约被强制验证。

**契约约束（Score_test.py 会强制验证）：**

| 约束名 | 说明 |
|--------|------|
| 保序 | `prob_a < prob_b` 时分数方向全程一致，贯穿量化步骤 |
| 可复现 | 先用 `proba_round_decimals` 量化掉浮点尾数，同一业务概率在 fp32/bf16/跨平台统一分数 |
| 纯函数 | 同输入同输出，无外部状态依赖 |
| 全域 | 闭区间 [0,1]（含 0.0/1.0）有效；NaN / <0 / >1 → ValueError |
| 有界 | 输出 ∈ [`score_range[0]`, `score_range[1]`] |

**Score.py 结构示例：**

```python
"""Score conversion. proba_pos ∈ [0,1] → int."""
Score_version: str = "2026-05-26"
Score_range: tuple[int, int] = (300, 999)
Positive_class: int = 1           # 必须与 io_schema.outputs.positive_class 一致
n_classes_expected: int = 2
proba_round_decimals: int = 6     # 必填：映射前先量化的小数位

def stretch(prob_pos: float) -> int:
    """先量化再映射，杜绝浮点尾数导致的分数跳桶。
    p = round(prob_pos, proba_round_decimals)  # 第一步：量化
    # 再映射到 [Score_range[0], Score_range[1]]
    # 允许 import：仅 numpy / 标准库 math
    """
    ...
```

---

#### 4.1.7 合规校验规则

**硬性（阻塞合规，必须全部通过）：**

| 校验项 | 规则 |
|--------|------|
| 必备件齐全 | `pkl` + `io_schema.json` + `source_meta.json` + `test_cases.json` 四文件均存在 |
| 特征数一致 | `len(io_schema.inputs)` == `source_meta.shape.n_features` |
| sha256 三重一致 | `source_meta.pkl_sha256` == `sha256(pkl文件)` == `test_cases.generated_with.pkl_sha256` |
| expected_proba 维度 | 每个 case 的 `expected_proba` 长度 == `n_classes` |
| Score 结构（当 Score.py 存在时） | 必须配套 `Score_test.py` + `Positive_class` 与 `io_schema` 一致 |

**软性（仅警告，不阻塞）：**

| 校验项 | 规则 |
|--------|------|
| Score.py 按需配置 | 可不存在 |
| categorical 类别枚举 | 填写完整枚举集（否则目标平台可能编码不一致） |
| positive_class_meaning | 填写业务含义描述 |
| test_cases 非空 | 有真实业务样例 |
| 目录名匹配 | 目录名与 meta 中 naming 字段匹配 |

---

### 4.2 非功能需求

#### 4.2.1 文件交付要求

- 所有 sidecar JSON 文件须包含 `schema_version: 1` 字段。
- pkl 文件名须与所在目录名相同（同名原则）。
- JSON 文件使用 UTF-8 编码，禁止 BOM 头。
- `test_cases.json` 可为空数组，但文件本身必须存在。

#### 4.2.2 故障处理机制

| 异常场景 | 处理方式 |
|----------|----------|
| sha256 不一致 | 立即阻断加载，提示「模型文件已损坏或被替换，请重新从源平台导出」 |
| n_estimators 不一致 | 警告提示两端配置差异，要求对齐后重新导出 |
| Score.py 存在但无 Score_test.py | 硬性阻断，必须补充测试文件 |
| positive_class 未填写但 Score.py 存在 | 硬性阻断，防止正类逆序风险 |
| dtype 与目标平台不一致 | 警告提示精度差异，参考 test_cases tolerance 中的 `proba_abs_bf16` 容差（6e-3） |

### 4.3 需求与责任分配表

> 以下责任人待定，评审时确认各方负责人后填写。

| 序号 | 所属模块 | 责任团队 | 责任人 |
|------|----------|----------|--------|
| 1 | LDM 文件打包 | 算法/数据科学团队 | 待定 |
| 2 | io_schema.json 生成 | 算法/数据科学团队 | 待定 |
| 3 | source_meta.json 生成 | 算法/数据科学团队 | 待定 |
| 4 | test_cases.json 生成 | 算法/数据科学团队 | 待定 |
| 5 | Score.py 开发（可选） | 算法/数据科学团队 | 待定 |
| 6 | 合规校验器 | 平台研发团队 | 待定 |
| 7 | 目标平台接入验证 | 平台研发 + QA | 待定 |

---

## 5. 技术方案

由技术团队在评审前完成填写。

**关键待确认项：**
- [ ] `n_estimators` 值（向强哥确认，1 或 4）
- [ ] `device` 类型（cpu / gpu，确认后更新 test_cases）
- [ ] 各环境 numpy / python / torch 版本约束范围

---

## 6. 任务拆解与里程碑

| 关键任务 | 负责方 | 交付时间 |
|----------|--------|----------|
| 确认 n_estimators 值（向强哥确认） | 算法团队 | 待定 |
| 确认推理 device 类型 | 平台研发 | 待定 |
| 开发 sidecar 文件自动生成工具 | 算法/平台研发 | 待定 |
| 开发合规校验器（硬性规则） | 平台研发 | 待定 |
| 源平台存量模型补打 sidecar | 算法团队 | 待定 |
| 目标平台验证与上线 | 平台研发 + QA | 待定 |

---

## 7. 资源与预算

待评审后补充实际人力与计算资源估算。

---

## 8. 风险与应对

| 风险编号 | 风险描述 | 风险等级 | 影响范围 | 应对措施 | 责任方 |
|----------|----------|----------|----------|----------|--------|
| R-01 | `n_estimators` 数值不一致：源平台与目标平台设置不同（1 vs 4），导致推理结果差异 | 高 | 模型输出质量 | 迁移前确认强哥设置的值，写入 source_meta.json 并作为合规必检项 | 算法负责人 |
| R-02 | dtype 精度差异：源平台 float32 vs 目标平台 bf16，概率值偏差约 6e-3，可能导致分值跨桶 | 中 | 分值准确性 | test_cases 记录生成时的 dtype；目标平台复现时同 dtype；Score.py 用 `proba_round_decimals` 量化消差 | 算法 + 平台研发 |
| R-03 | device 未确认：CPU 与 GPU 推理结果存在细微差异 | 低 | 分值准确性 | `test_cases.generated_with.device` 字段明确记录生成设备，目标平台对齐 | 平台研发 |

---

## 9. 测试方案

由 QA 补充完整测试方案，以下为验收标准框架：

| 测试用例 | 验收标准 |
|----------|----------|
| TC-01 | 合规校验器：对标准 LDM 包执行硬性校验全部通过 |
| TC-02 | 合规校验器：对缺少必备件的包返回明确错误码 |
| TC-03 | sha256 三重一致性：修改 pkl 后合规校验器报错 |
| TC-04 | 特征顺序验证：打乱 io_schema inputs 顺序后，平台给出警告 |
| TC-05 | 正类逆序防护：交换 positive_class 定义后，Score_test.py 中保序校验失败 |
| TC-06 | test_cases 回归：目标平台加载后，float32 精度下 proba 与期望完全一致 |
| TC-07 | n_estimators 一致性：源/目标平台 n_estimators 不同时，合规校验器输出警告 |

---

## 10. 附件清单

- LDM 技术规范文档（本文档依据来源）
- TabPFN 开源项目说明文档
- Score.py 参考实现模板
- sidecar 文件生成工具使用说明（待补充）

---

## 11. 签署页

本文档经以下各方确认后正式生效。

| 角色 | 姓名 | 签字 / 确认日期 |
|------|------|-----------------|
| 需求提出方 | 待确认 | ☐ 待确认 |
| 技术负责人 | 待确认 | ☐ 待确认 |
| 测试负责人 | 待确认 | ☐ 待确认 |
| 运维负责人 | 待确认 | ☐ 待确认 |
| 安全负责人 | 待确认 | ☐ 待确认 |

---

*百擎平台 · LDM 离线模型文件规范 PRD · 文档编号 LDM-TN-2026-001 · 草稿版本*
