# 5 个生成函数参数解释

---

## 1. generate_io_schema — 生成 io_schema.json

### 函数签名

```python
def generate_io_schema(X, y, feature_dtypes=None)
```

### 传参说明

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| `X` | DataFrame 或 ndarray | 必填 | 训练集特征，即模型训练时用的 X |
| `y` | array-like | 必填 | 训练集标签，即模型训练时用的 y（0/1） |
| `feature_dtypes` | dict，如 `{'age': 'numeric', 'city': 'categorical'}` | 选填 | 手动指定每个特征的类型。**不填则自动推断**：int/float 列 → numeric，其他 → categorical |

### 调用示例

```python
schema = generate_io_schema(df1w[feats100], df1w['other_var3'])
#                           ↑训练集特征    ↑训练集标签
```

### 逐行逻辑

```python
# 第1步：获取特征名
if hasattr(X, 'columns'):          # 如果 X 是 DataFrame
    feature_names = X.columns.tolist()   # 取列名
else:                              # 如果 X 是 ndarray
    feature_names = [f"feat_{i}" for i in range(X.shape[1])]  # 自动生成 feat_0, feat_1...

# 第2步：推断特征类型（未传 feature_dtypes 时执行）
for col in feature_names:
    dtype = str(X[col].dtype)      # 取该列的数据类型字符串，如 'float64'、'object'
    if dtype in ['int64', 'float64', 'int32', 'float32']:
        feature_dtypes[col] = 'numeric'     # 数值型 → numeric
    elif dtype == 'bool':
        feature_dtypes[col] = 'boolean'     # 布尔型 → boolean
    else:
        feature_dtypes[col] = 'categorical' # 其他（字符串等）→ categorical

# 第3步：构建每个特征的定义
input_def = {
    "name": col,                        # 特征列名
    "dtype": feature_dtypes[col],       # 类型（numeric/categorical）
    "required": True,                   # 固定 true
    "missing_allowed": True             # 固定 true
}

# 第4步：categorical 特征额外提取全量枚举值
if dtype == "categorical":
    cats = X[col].dropna().unique().tolist()   # 去掉 NaN 后取所有不重复值
    input_def["categories"] = [str(c) for c in cats]  # 转字符串保证 JSON 兼容
    if len(cats) > 100:
        print(f"Warning: ...")   # 类别超过 100 个打印警告

# 第5步：确定正类
unique_labels = np.unique(y)           # 取标签的所有唯一值，如 [0, 1]
positive_class = int(max(unique_labels))  # 取最大值作为正类，二分类下即 1
```

### 完整调用与保存

```python
import json
import numpy as np

# 调用函数生成 schema
schema = generate_io_schema(df1w[feats100], df1w['other_var3'])
#                           ↑训练集特征        ↑训练集标签

# 保存为 JSON 文件
with open('/opt/.../io_schema.json', 'w', encoding='utf-8') as f:
    json.dump(schema, f, indent=2, ensure_ascii=False)
# ⚠️ 输出格式强制限定：indent=2, ensure_ascii=False，不要修改

print("已保存 io_schema.json")
print(json.dumps(schema, indent=2, ensure_ascii=False))
```

### 注意事项

- `positive_class_meaning` 硬编码为 `"default / bad"`，如需修改在函数内手动改
- 当前业务场景所有特征均为 numeric，categorical 分支基本不走
- 调用时 X 必须是训练集，不能是测试集，否则特征列顺序可能不对
- **保存格式（indent=2, ensure_ascii=False）有强制限定，不要改变**

---

## 2. generate_source_meta — 生成 source_meta.json

### 函数签名

```python
def generate_source_meta(pkl_path, X, business_scene="credit_risk", arch=None)
```

### 传参说明

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| `pkl_path` | str | 必填 | pkl 文件的完整路径，如 `/opt/.../model.pkl`。用于计算 sha256 和加载模型 |
| `X` | DataFrame 或 ndarray | 必填 | 训练集特征，用于读取特征数量（n_features）和样本量（n_train_rows） |
| `business_scene` | str | 选填，默认 `"credit_risk"` | 业务场景名称，写入 naming.business_scene 字段 |
| `arch` | str | 选填，默认 None | 模型架构版本，当前函数内已注释掉，默认返回 "unknown"（实际固定为 V2.5），使用其他架构时取消注释并传入 |

### 调用示例

```python
meta = generate_source_meta(
    pkl_path='/opt/.../credit_risk_tab2.5_n1_f100_10000.pkl',
    X=X_train,
    business_scene="credit_risk"
)
```

### 逐行逻辑

```python
# 第1步：计算 pkl 文件的 SHA-256 哈希
def file_sha256(filepath):
    sha256 = hashlib.sha256()
    with open(filepath, 'rb') as f:
        for chunk in iter(lambda: f.read(4096), b''):  # 每次读 4096 字节
            sha256.update(chunk)   # 累积更新哈希
    return sha256.hexdigest()      # 返回 64 位十六进制字符串

# 第2步：加载模型，读取 n_estimators
classifier = joblib.load(pkl_path)
n_estimators = getattr(classifier, 'n_estimators', 1)
# getattr 第3个参数是默认值，读不到 n_estimators 属性时默认取 1

# 第3步：自动读取各库版本号
import sklearn; sklearn_version = sklearn.__version__
import tabpfn;  tabpfn_version  = tabpfn.__version__
# numpy、python、torch 版本同理，读取当前运行环境的实际版本

# 第4步：尝试从模型对象读取 arch（当前被注释，实际不执行）
arch = getattr(classifier, 'arch', None)          # 尝试读 arch 属性
arch = getattr(classifier, 'model_arch', None)    # 尝试读 model_arch 属性
arch = getattr(classifier, 'architecture', None)  # 尝试读 architecture 属性
# 三个都读不到时 arch = "unknown"

# 第5步：组装 meta 字典
"pkl_sha256": file_sha256(pkl_path)        # sha256 值，三重一致性校验锚点
"captured_at": datetime.now().strftime(...)# 当前时间（UTC），自动生成
"env_capture": "single-env workflow..."    # 固定字符串，说明训练环境与捕获环境相同
"source.kind": "LDM-tn"                   # 固定值，本格式的标识符
"shape.n_features": X.shape[1]            # 训练集列数，从 X 的维度直接读
"shape.n_train_rows": X.shape[0]          # 训练集行数，从 X 的维度直接读
```

### 完整调用与保存

```python
import json

pkl_path = '/opt/.../credit_risk_tab2.5_n1_f100_10000.pkl'

# 调用函数生成 meta
meta = generate_source_meta(pkl_path, X_train, business_scene="credit_risk")

# 保存为 JSON 文件
output_path = '/opt/.../source_meta.json'
with open(output_path, 'w', encoding='utf-8') as f:
    json.dump(meta, f, indent=2, ensure_ascii=False)

print("已保存 source_meta.json")
print(json.dumps(meta, indent=2, ensure_ascii=False))
```

### 注意事项

- `arch` 参数当前被注释，若使用 V3 等其他架构，需在函数内取消注释并传入正确值
- 版本号从**运行时环境**自动读取，需确保生成文件时的环境与训练时一致

---

## 3. generate_test_cases — 生成 test_cases.json

### 函数签名

```python
def generate_test_cases(pkl_path, X, y, feature_names=None, n_cases=1)
```

### 传参说明

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| `pkl_path` | str | 必填 | pkl 文件完整路径，用于加载模型和计算 sha256 |
| `X` | DataFrame 或 ndarray | 必填 | 训练集特征，**当前固定取第 0 行**作为测试样本 |
| `y` | array-like | 必填 | 训练集标签（当前函数内未实际用到，但接口保留） |
| `feature_names` | list | 选填，默认 None | 特征名列表。不填时自动从 X 的列名获取（DataFrame）或生成 feat_0, feat_1...（ndarray） |
| `n_cases` | int | 选填，默认 1 | 生成的测试用例数量。**当前代码内部固定取索引 [0]，此参数暂未生效** |

### 调用示例

```python
test_cases = generate_test_cases(
    pkl_path='/opt/.../model.pkl',
    X=X_train,
    y=y_train,
    feature_names=None,  # 自动从 X 取列名
    n_cases=1
)
```

### 逐行逻辑

```python
# 第1步：加载模型
classifier = joblib.load(pkl_path)
if hasattr(classifier, 'eval'):
    classifier.eval()   # 如果模型有 eval 方法，切换到推理模式（PyTorch 惯例）

# 第2步：取第 0 行作为测试样本
indices = [0]   # 当前写死取第 0 行，n_cases 参数暂未生效
row = X.iloc[0]
input_dict = {name: to_python_type(row[name]) for name in feature_names}
# to_python_type 的作用：把 numpy 数据类型（np.float32 等）转为 Python 原生类型（float/int）
# 这样才能正确序列化为 JSON

# 第3步：模型推理
x_single = X[0:1].values.astype(np.float32)  # 取第 0 行，转为 float32 的 ndarray
proba = classifier.predict_proba(x_single)    # 推理，返回形如 [[0.55, 0.45]]
case = {
    "input": input_dict,
    "expected_proba": [round(float(p), 6) for p in proba[0]]  # 保留 6 位小数
}

# 第4步：检测推理 dtype
params = list(classifier.model.parameters())
dtype = str(params[0].dtype).replace('torch.', '')  # 读模型参数的实际精度，如 'float32'

# 第5步：检测推理 device
device = "cuda" if torch.cuda.is_available() else "cpu"
# 有 GPU 就写 cuda，没有写 cpu
# 百基平台的推理设备必须与此一致，否则结果可能不同
```

### 完整调用与保存

```python
import json

pkl_path = '/opt/.../credit_risk_tab2.5_n1_f100_10000.pkl'

# 调用函数生成 test_cases
test_cases = generate_test_cases(
    pkl_path=pkl_path,
    X=X_train,
    y=y_train,
    feature_names=None,  # 自动从 X 取列名
    n_cases=1
)

# 保存为 JSON 文件
output_path = '/opt/.../test_cases.json'
with open(output_path, 'w', encoding='utf-8') as f:
    json.dump(test_cases, f, indent=2, ensure_ascii=False)

print(json.dumps(test_cases, indent=2, ensure_ascii=False))
```

### 注意事项

- **当前只生成 1 条用例（第 0 行），n_cases 参数暂未生效**
- NaN 值会原样保留在 input 中，不做填充
- 生成的文件**不含 expected_score**，需要分值时另行处理
- device 是百基接入的关键对齐字段，生成后不要手动修改

---

## 4. score.py — 3 个拉伸函数

### 4.1 function_ls — 训练阶段拉伸验证

```python
def function_ls(prob_series, target_min, target_max, mode='L')
```

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| `prob_series` | pd.Series 或 ndarray | 必填 | 模型输出的正类概率列，值域 [0, 1]。传入**训练集全量概率**，用于拟合映射系数 |
| `target_min` | int | 必填 | 目标分值区间下限，如 300、500 |
| `target_max` | int | 必填 | 目标分值区间上限，如 999、1000。**必须大于 target_min，不允许负数** |
| `mode` | str | 选填，默认 `'L'` | `'L'`（Low=低分低风险）：概率越高，分值越高；`'H'`（High=低分高风险）：概率越高，分值越低 |

### 逐行逻辑

```python
if mode == 'H':
    probs = 1 - probs     # H 模式：把概率反转，高概率变低概率，再正向映射

c = target_min            # 二次函数常数项 = 区间下限（概率=0时的分值）
delta = target_max - target_min   # 区间总宽度
mean_prob = np.mean(probs)        # 训练集概率均值，用作锚点
mid = (target_min + target_max) / 2.0  # 分值区间中点

# 用均值概率锚定中点，建立方程组，求解 a 和 b
# 约束：score(mean_prob) = mid（均值概率映射到分值中点）
numerator = mid - c - delta * mean_prob
denominator = mean_prob**2 - mean_prob
a = numerator / denominator       # 二次项系数
b = delta - a                     # 一次项系数

# 安全检查：若 b<0 或 (2a+b)<0，函数单调性被破坏，退化为线性映射
if b < 0 or (2*a + b) < 0:
    a = 0.0
    b = delta    # 线性映射：score = delta*p + target_min

a = round(a, 4)   # 系数保留 4 位小数
b = round(b, 4)
c = round(c, 4)

mapped = a * (probs ** 2) + b * probs + c  # 套公式计算
mapped = np.round(mapped).astype(int)      # 四舍五入取整
```

### 完整调用示例

```python
import seaborn as sns

# 训练阶段验证：直接拉伸并查看分布
df['score1'] = function_ls(df['prob'], target_min=500, target_max=1000, mode='H')
sns.distplot(df['score1'], bins=100)  # 查看分值分布是否合理

df['score2'] = function_ls(df['prob'], target_min=320, target_max=1000, mode='L')
sns.distplot(df['score2'], bins=100)
```

---

### 4.2 train_ls_coeff — 固化系数（生产用）

```python
def train_ls_coeff(prob_series, target_min, target_max, mode='L')
```

| 参数 | 说明 |
|------|------|
| `prob_series` | 同 function_ls，传入训练集全量概率 |
| `target_min` | 同 function_ls |
| `target_max` | 同 function_ls |
| `mode` | 同 function_ls |

**与 function_ls 的区别：** 只计算并返回系数字典，不做实际映射。

```python
return {'a': a, 'b': b, 'c': c, 'target_min': target_min, 'target_max': target_max, 'mode': mode}
# 返回的字典就是打分规则的完整描述，存起来供 apply_ls_score 使用
```

**生产部署流程：**
```
训练完成后 → train_ls_coeff 固化系数 → 把系数字典存起来
百基推理时 → apply_ls_score(新概率, 系数字典) → 输出分值
```

---

### 4.3 apply_ls_score — 推理阶段打分（百基用）

```python
def apply_ls_score(prob, coeff_dict)
```

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| `prob` | float 或 ndarray | 必填 | 单个概率值或概率数组，取模型输出的正类概率 |
| `coeff_dict` | dict | 必填 | 由 `train_ls_coeff` 返回的系数字典，包含 a/b/c/mode |

### 逐行逻辑

```python
a = coeff_dict['a']      # 读取二次项系数
b = coeff_dict['b']      # 读取一次项系数
c = coeff_dict['c']      # 读取常数项（= target_min）
mode = coeff_dict['mode']# 读取模式

is_scalar = np.isscalar(prob)        # 判断传入的是单个值还是数组
x = np.asarray([prob]) if is_scalar else np.asarray(prob)

if mode == 'H':
    x = 1 - x    # H 模式下先反转概率

scores = a * (x ** 2) + b * x + c   # 套公式
scores = np.round(scores).astype(int) # 四舍五入取整

return float(scores[0]) if is_scalar else scores
# 输入单个值 → 返回单个 float；输入数组 → 返回数组
```

---

## 5. score_test.py — 生成验证用例

score_test.py 不是一个独立函数，而是一段脚本，依赖 score.py 中的 `train_ls_coeff` 和 `apply_ls_score`。

### 脚本逐段说明

```python
# 第1步：固化 L 模式和 H 模式两套系数
coeff_L = train_ls_coeff(df['prob'], target_min=550, target_max=1000, mode='L')
coeff_H = train_ls_coeff(df['prob'], target_min=550, target_max=1000, mode='H')
# df['prob']：训练集的正类概率列
# target_min/target_max：用户填写的分值区间
# mode：'L'=高概率高分，'H'=高概率低分

# 第2步：指定要测试的样本索引
indices = [1, 10]   # 取第 1 和第 10 行的概率值作为测试样本

# 第3步：对 L 模式逐样本生成测试用例
for idx in indices:
    prob_val = df['prob'].iloc[idx]            # 取该行的概率值
    score_L = apply_ls_score(prob_val, coeff_L)# 用 L 模式系数打分
    test_cases.append({
        "input":    {"prob": float(prob_val)},  # 输入的概率值
        "output":   {"score": int(score_L)},    # 期望输出的分值
        "metadata": {"target_min": 300, "target_max": 1000, "mode": "L"}  # 参数配置
    })

# 第4步：对 H 模式同样生成（同上，mode 不同）

# 第5步：写入 JSON 文件
with open('/opt/.../score_test.json', 'w') as f:
    json.dump(test_cases, f, indent=2)

print(json.dumps(test_cases, indent=2))
```

### 关键说明

| 字段 | 说明 |
|------|------|
| `input.prob` | 测试用的概率值，取自训练集指定行 |
| `output.score` | 用固化系数算出的期望分值，百基平台复现后必须与此一致 |
| `metadata.mode` | 百基读取时按此字段判断用哪套系数。`mode='H'` 时需先做 `p = 1-p` |
| `metadata.target_min/max` | 当前脚本里写死为 300/1000，**需与实际业务区间保持一致** |

---

*5 个生成函数参数解释 · 2026-06-04*
